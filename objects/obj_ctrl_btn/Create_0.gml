
// Variables globales en el crear de obj_ctrl_btn
mostrar_pregunta = false;
pregunta_origen = noone; // Guardará la instancia del objeto que activó la pregunta

// Inicializar otras variables
seleccionada = -1;
evitar_clic = true;
alarm[0] = 5;  // Activar un pequeño retraso para permitir la interacción
